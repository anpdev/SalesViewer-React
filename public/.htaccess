<IfModule mod_rewrite.c>
    RewriteEngine On
	
    # Send /home/cor/master/app/build/index.html
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.html [L]
</IfModule>
